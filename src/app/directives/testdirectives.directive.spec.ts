import { TestdirectivesDirective } from './testdirectives.directive';

describe('TestdirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new TestdirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
